<?jelly escape-by-default='true'?>
<div>
    Adds a build parameter that lists Git branches from a configured repository, ordered by recent
    commit activity so the branches your team is working on come first. Supports regex filtering,
    a configurable limit, rules to always show or to disable specific branches, and Jenkins
    credentials for private repositories.
</div>
